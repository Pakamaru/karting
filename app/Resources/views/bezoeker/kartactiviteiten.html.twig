{% extends 'bezoeker.html.twig' %}
{% block menu %}
    <li><a href="{{ path('homepage') }}">home</a></li>
    <li class="active"><a href="{{ path('kartactiviteiten') }}">aanbod</a></li>
    <li><a href="{{ path('registreren') }}">registreren</a></li>
    <li><a href="#">contact</a></li>
{% endblock %}
{% block content %}
    <section >
        <article>
            <h3>Er zijn {{ soortactiviteiten|length }} soorten activiteiten </h3>
            <table>
                <tr>
                    <th class="list-group-item text-primary">Naam</th>
                    <th class="list-group-item text-primary">Minimale leeftijd</th>
                    <th class="list-group-item text-primary">Tijdsduur</th>
                    <th class="list-group-item text-primary">Prijs</th>
                </tr>
                {% for activiteit in soortactiviteiten %}
                    <tr>
                        <td class="list-group-item">{{ activiteit.naam }}</td>
                        <td class="list-group-item">{{ activiteit.minLeeftijd }} jaar</td>
                        <td class="list-group-item">{{ activiteit.tijdsduur }} minuten</td>
                        <td class="list-group-item">&euro;{{ activiteit.prijs }}</td>
                    </tr>
                {% endfor %}
            </table>
            <p>
                Let op, voor het karten zijn dichte schoenen verplicht.
                Karten met een korte broek is niet toegestaan. Wij hebben overalls ter beschikking,
                maar probeer indien mogelijk een lange broek aan te doen of mee te nemen.
            </p>
        </article>
        <figure>
            <img class="img-responsive center-block" style="width:30%"src={{ asset('img/kart.jpg') }} alt="kart" />
        </figure>
    </section>
{% endblock %}